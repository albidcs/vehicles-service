/**
 * Application layer for vehicle use cases.
 *
 * <p>Contains services that orchestrate domain logic, enforce transactional
 * boundaries, and coordinate between domain and external layers via ports.
 */
package io.github.albi.vehicles.application.vehicle;